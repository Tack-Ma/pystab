from setuptools import setup, find_packages

setup(
    name='pystab',
    version='0.1',
    packages=find_packages(),
    license='',
    author='takuma',
    author_email='',
    description='',
    requires=['numpy', 'sympy']
)
